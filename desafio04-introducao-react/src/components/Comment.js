import React, { Component } from "react";

// Responsável por exibir um comentário, a partir do componente Post.
function Comment({ data }) {
  return <li>Isto é o Comment</li>;
}
export default Comment;
